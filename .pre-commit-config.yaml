repos:
  - repo: local
    hooks:
      - id: just-fmt
        name: just fmt
        entry: just fmt
        language: system
        pass_filenames: false
        always_run: true
      - id: just-lint
        name: just lint
        entry: just lint
        language: system
        pass_filenames: false
        always_run: true
